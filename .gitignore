# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

bin/
coverage.out

# Claude Code session scheduler state (transient, machine-local)
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
