# docs-reality-check allowlist: substrings of "path:lineno:content" lines that
# the stale-claim scan should SUPPRESS because they are legitimate planning prose
# about genuinely-unshipped behavior (not drift about a shipped command).
# One substring per line; blank lines and #-comments are ignored.
# Keep this list minimal and justify each entry — an over-broad entry hides real drift.
