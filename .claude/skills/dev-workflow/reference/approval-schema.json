{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "approved": {
      "type": "boolean",
      "description": "Whether to approve this phase and proceed"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence in the decision (0-1). Below threshold triggers human escalation"
    },
    "reason": {
      "type": "string",
      "description": "Brief explanation of the decision"
    },
    "concerns": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Specific concerns that lowered confidence or caused rejection"
    }
  },
  "required": ["approved", "confidence", "reason"]
}
