# Final Review — etude-egg (retros: config block)
Verdict: GO. Coherent (parse/validate/expose; advisory; auto-fire deferred; matches design §4). Both invariants hold (Retros==nil<=>absent; present-any-form->validated) verified vs decodeRetrosNode + accessors + validateRetros + regression tests. Default-true correct, no mutation. Nested KnownFields preserved (re-marshal+fresh KnownFields decoder rejects unknowns at both levels); goldenDefaultYAML untouched. toWorkflow (Workflow,error) signature change contained (only ParseYAML calls it). docs/init.md retros: subsection accurate (advisory/non-gating/defaults/generator-required/present-vs-absent). 1-bead=1-commit, 3 files, clean scope.
Gate history (all real codex/gemini catches the repo-aware seat missed): plan r1 BLOCK (synthesize-on-absent lost presence bit + broke round-trip) -> r2 PASS (nil-for-absent + accessors); implement r1 BLOCK (present-null *ptr decoded to nil == absent) -> r2 PASS (yaml.Node presence detection).
Commit: add optional retros block to workflow.yaml schema (351a080).
