# etude-5dr.7 documentation

`docs/run.md` now states that Git control and effective ignore-control files must be regular and that tracked paths may not pass through symlinked parent directories; both topology substitutions fail closed even when visible bytes match.
