Proportionality: The bead serves issue #17 ask 3 at stated priority. Existing manifests and artifact storage supply source facts, but no existing primitive assembles rerun evidence. Two- and three-attempt live paths provide measured scope. The redaction threat is reachable through untrusted producer transcripts.

Cohesion: The diff injects digest-bound prior-attempt evidence into every gate rerun.

Correctness: Evidence is deterministically rebuilt from durable history, namespace collisions fail before execution, transcript states enforce safe import, resume restores logs, and independent goldens verify payloads. No irreversible step, concurrent invoker, veto race, crash-prefix sequence, identity proxy, or undeclared blast radius exists.

VERDICT: PASS
