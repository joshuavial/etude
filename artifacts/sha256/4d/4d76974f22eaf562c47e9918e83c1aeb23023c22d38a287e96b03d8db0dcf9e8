{"verdict": "go", "required": [], "optional": ["1. Clarify in the plan that `--stage <name>` is resolved against the workflow before using `stage.Produces`; the tests imply this, but the step list skips the lookup."]}