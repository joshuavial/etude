## Docs — etude-phase2.2 (Changed: no)

No shipped docs needed. internal/worktree is an internal package with no CLI surface (grep: zero importers outside the package; no etude command wired). Shipped docs (capture/init/run/sync/README) describe user-facing commands; none affected. capture.md's 'replay not implemented yet' note stays truthful (worktree is just a building block). Worktree/replay discussion is under docs/plans/. User-facing replay docs + the capture.md Current Limits update are deferred to etude-replay-command.
