The corrected plan is proportional and cohesive, but three defects remain. A recovered gate can inherit the previous Stage's flat internal bundle unless the durable ordinary-input source is specified. Treating transient transcript retrieval failure like redaction failure permanently bricks the run without a safety benefit. Contemplated collisions between workflow and internal roles make index references ambiguous. Reserve the internal namespace, define recovery filtering from that namespace, and fail closed only for unsafe unredacted evidence.

Follow-ups: pin seat order, eligible-attempt scope, and legacy empty-status normalization.

VERDICT: BLOCK
