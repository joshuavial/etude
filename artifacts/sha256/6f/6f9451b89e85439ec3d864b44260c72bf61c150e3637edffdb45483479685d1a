# etude-5dr.6 implementation round 2

Commit `15e5911` closes the caller-provenance reachability seams with commit peeling, replacement-disabled original-A and prior-caller ancestry, Git control snapshots including `info/grafts` and the configured external excludes file, and independent no-publication regressions. It preserves the documented allowance for ignored runner artifacts while still rejecting mutation of an active external excludes file.
