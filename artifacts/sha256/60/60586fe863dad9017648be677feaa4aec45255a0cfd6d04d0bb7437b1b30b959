The new grammar accepts exactly non-empty numeric components separated by single
dots. Empty, leading dot, trailing dot, repeated dot, and non-digit inputs reject;
valid multi-component versions remain accepted. The regression exercises the
intended seam.

VERDICT: PASS_WITH_FOLLOWUPS
