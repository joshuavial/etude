# Final Review — etude-github-import
PLAN gate (Tier 1, codex/gemini/opus + pilms-down): r1 codex+gemini BLOCK (stage honesty — final diff mapped to an `implement` stage misrepresents post-review code; brief says imported PRs are review/docs-only); amended to review-only (no plan/implement); r2 codex+gemini+opus GO.
IMPLEMENT gate (Tier 1): r1 codex BLOCK (buildImportRunID didn't collapse `..` → could produce an IsValidRunID-invalid id; +2 NITs: preflight gh-presence outside the injected client → tests needed gh on PATH; README example block lacked import) / gemini GO / opus GO (live-drive verified). Fixed: dots-collapse + IsValidRunID skip-guard + preflight-through-client (hermetic) + README example. r2 codex GO.
Scope: new internal/cli/import.go + test + testdata + root.go + prime.go (import removed from Planned) + docs/import.md + generated docs/cli/etude_import.md + README + docs/README. Reuses runmanifest/artifactstore/refstore. gh CLI only, NO token. Read-only against GitHub; create-only local refs (no auto-push). Post-v1 Phase-1 (V1-SCOPE untouched). Verdict: GO.
