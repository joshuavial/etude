# Verify — etude-phase-prep.3 (Define degraded review-gate policy) — PASS
- Process-docs only: review-gate-runbook.md (new consolidated "## Degraded Gate Policy" section + fallback-paragraph replaced by a pointer + 2 stray refs renamed) + review-gate-process.md (bounded-exception carve-out). No code.
- make docs-reality PASS; make docs-check PASS; scope = exactly the 2 process docs.
- Strict default preserved (unanimous substantive GO; substantive BLOCK never bypassed) in BOTH docs. Disregard bounded to a single root-caused tooling-outage seat (>=2 novel rerolls; single-confirming only for already-known artifacts), other three unanimous GO. Disproven BLOCK routed to the disputed-claim rule, NOT disregard. Degraded 3-seat pass orchestrator-authorized in autonomous loop else escalate; recorded structurally.
- Schema mapping uses only shipped enums (malfunction/failed/empty/disregarded + decision.degraded_reason; gate status pass) — no new field. No competing "tooling-outage fallback" heading remains.
- Acceptance 1-5 all met (verified by Opus + codex).
