{"verdict": "go", "required": [], "optional": ["1. `.etude/run-map.tsv` is space-separated despite the `.tsv` name; the parser accepts either, but rename or use tabs so the format matches the filename. 2. In both profiles `review` is mandatory while `routine-review` is optional, yet docs/development.md says to pick exactly one; if capture/gate enforces stage order via frontier, a routine lane that skips `review` may be rejected. Make `review` optional or document the ordering behaviour. 3. The audit script header comment for check (a) still says `refs/etude/runs/<id>` with no mention of the run-map override; the ResolveCheckRunner/ResolveGateSeat comments likewise predate seat-specific env additions (QA already noted this). 4. `pass_threshold: 1` is documented as unanimity; the packet does not show the workflow schema semantics (fraction vs count), so a one-line note in docs/gates.md stating the interpretation would remove ambiguity for future editors."], "session": {"session_id": "fable-2057bab29f64b73c", "transcript_path": "fable-transcript.txt"}}