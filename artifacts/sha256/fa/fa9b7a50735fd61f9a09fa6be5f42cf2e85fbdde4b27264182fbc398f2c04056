# etude-5dr.6 implementation round 4

Commit `39a7559` is the final bounded guard correction. It resolves relative external-exclude paths from the guarded repository and snapshots case-folded untracked `.gitignore` names, with seam-specific no-publication regressions. Together with prior fixes, ordinary ignored artifacts remain allowed while mutation of every identified active ignore control fails closed.
