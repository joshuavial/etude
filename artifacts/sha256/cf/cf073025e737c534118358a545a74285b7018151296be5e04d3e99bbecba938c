VERDICT: BLOCK
BLOCKING: 1. `internal/replay/record.go`: `buildSessionEvidence` imports and secret-scans the transcript, but on `ScanForSecrets` failure it only records failed redaction evidence and still records the replay run; per the documented contract in `docs/run.md`, an imported transcript that fails the scan must stop before recording/continuing, so this can persist an unsafe run artifact as-is.
OPTIONAL: none
CONFIDENCE: medium
