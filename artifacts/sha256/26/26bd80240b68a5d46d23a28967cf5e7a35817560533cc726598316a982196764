The corrected plan is proportional, cohesive, reuses existing manifest/CAS primitives, and covers deterministic ordering, complete evidence, replacement semantics, transcript-import failure, non-empty session reuse, independent golden expectations, serial CAS publication, and digest-sharing without role loss.

Follow-up: explicitly declare whether arbitrary same-process code execution is in scope.

VERDICT: PASS_WITH_FOLLOWUPS
