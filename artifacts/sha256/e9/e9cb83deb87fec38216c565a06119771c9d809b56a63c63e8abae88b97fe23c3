packet: .etude/tmp/etude-s6z.1/plan.packet.md
sha256: ff8b5d8622f6b478fc7b52286dc6374d39a6f7eb710ff36b9c59b0f46d426d04

Proportionality: The reviewer seat and ExecRunner inline command are existing primitives, but a producer adapter is genuinely missing. The planned change is cohesive and proportionate.

Blocking defects:
1. Cite and test the real pinned-worktree cwd guarantee for relative wrapper and agent resolution.
2. Pin the five literal stage-to-agent mappings, assert their checked-in definitions exist, and prove a non-plan adapter.
3. Reject empty or whitespace-only successful Claude output before publication.
4. Classify prior model artifacts as untrusted inputs and place their contents inside explicit data delimiters.

VERDICT: BLOCK
