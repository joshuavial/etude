Keep identifier character policy in internal/ident, but keep workflow-specific diagnostics in internal/workflow. Replace validateStageName with an explicitly required helper that accepts the stage prefix, emits the existing exact required error for empty or whitespace-only names, and emits the existing exact invalid-name error otherwise; call it without a separate blank guard and add direct table coverage for the helper API.
