VERDICT: GO
BLOCKING: none
OPTIONAL: 1. Consider adding a replay CLI integration test that proves `ETUDE_SESSION_FILE` is present end-to-end, not just at `ExecRunner`/recorder boundaries.
CONFIDENCE: medium
