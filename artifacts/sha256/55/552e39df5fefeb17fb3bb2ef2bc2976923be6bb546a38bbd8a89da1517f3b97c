The plan remains blocked because the transcript state machine does not reject `retrieval=imported`, `redaction=passed`, and no transcript artifact. That input would let the rerun proceed while falsely reporting safe imported evidence. Validate the artifact-absent side of the state machine as well as artifacts paired with bad status.

VERDICT: BLOCK
