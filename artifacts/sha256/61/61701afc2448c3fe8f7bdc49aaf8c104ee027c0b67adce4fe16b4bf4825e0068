VERDICT: GO
BLOCKING: none
OPTIONAL: 1. Dry-run's normal-path mirror preview does not call ValidateMirrorRemote, so a config-written remote name that cannot form a legal ref (e.g. `prod:backup`) previews `plan: configure` lines that the real run rejects; call the validator in the preview branch. 2. The dry-run mirror loop re-runs `gitGetAll` once per kind on the same fetch key; hoist the read above the loop. 3. `fmtMetadataSync` now prints an informational line on every init with an existing remote and no test pins it; either assert it or drop it to keep init output stable. 4. README, BRIEF and V1-SCOPE say init works "without changing branch push policy", which understates that init deletes the byte-exact local legacy push value; a short parenthetical pointing at the docs/init.md migration section would keep the summaries honest.
CONFIDENCE: high
