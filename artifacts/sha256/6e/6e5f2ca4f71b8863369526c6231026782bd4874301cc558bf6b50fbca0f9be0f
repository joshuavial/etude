VERDICT: BLOCK
BLOCKING: 1. `internal/replay/record.go`: `resolveRecordedTranscriptPath` accepts relative paths like `../outside` by joining them to `scratchDir` or `worktreeDir`; containment is only enforced later against the selected root, but for missing scratch files it falls through to worktree with no pre-normalization/rejection, so relative traversal can be reported as failed rather than clearly rejected and may rely on downstream behavior not shown in the artifact. 2. `internal/replay/record.go`: replay transcript evidence is only recorded when `producer.Harness.Name` is not `"shell"`, but the docs now state replay runner session capture generally; if the replay runner reports `ETUDE_SESSION_FILE` while the harness name defaults empty or shell semantics are used, behavior is ambiguous and can silently omit expected evidence.
OPTIONAL: 1. Add tests for failed secret scan, unreadable transcript, absolute path outside allowed roots, and relative path traversal. 2. Add a manifest test proving a gate-less run with a stage log emits version 4.
CONFIDENCE: medium
