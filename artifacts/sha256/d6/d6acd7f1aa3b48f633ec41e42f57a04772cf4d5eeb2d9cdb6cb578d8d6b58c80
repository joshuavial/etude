{"verdict": "go", "required": [], "optional": ["1) The excerpted manifest JSON omits the post-run SHA fields, so the console's `captured f475e3e\u2026` / `captured 6a1424f\u2026` cannot be cross-matched against the shown stage `git_sha` values from the artifact alone \u2014 include those fields in the PR Manual QA excerpt. 2) The final paragraph (sidecar transcript import, no-sidecar `producer_session: null`, collision workflow exit 1 with absent sentinel) is asserted in summary form only; paste the retained command output inline like the other scenarios so each claim carries its own evidence."], "session": {"session_id": "opus-1c74f766a9783782", "transcript_path": "opus-transcript.txt"}}