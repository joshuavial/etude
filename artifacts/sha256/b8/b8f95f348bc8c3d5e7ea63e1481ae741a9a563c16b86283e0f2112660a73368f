# etude-5dr.6 implementation round 3

Commit `6cae80c` closes the caller-provenance reachability seams with commit peeling, replacement-disabled original-A and prior-caller ancestry, and byte snapshots of Git control files, effective external excludes, and untracked `.gitignore` files. It preserves ordinary ignored runner artifacts while rejecting configured, default-global, whitespace-suffixed, and self-hidden ignore-control mutation; each negative test proves no publication.
