{
  "retro_type": "cadence",
  "original_event_date": "2026-05-26",
  "failure_modes": [
    "etude-7no `etude prime` drift guard took FOUR implement rounds as codex surfaced one collision class per round; only indent-anchored whole-token matching closed them",
    "an \"X appears in rendered output\" assertion using substring/Contains is silently satisfied by names that prefix (capture/capture-gate) or nest (run/run list)",
    "codex repeatedly caught real defects Opus + gemini GO'd: ojz symlink-clobber; jb7 path-containment + trailing-doc parse; 7no hallucinated flags + 3 drift-guard precision gaps",
    "7no's 4-round implement spiral was costly for a P3"
  ],
  "root_causes": [
    "output-membership guards used loose substring matching instead of whole-token + indent/position matching",
    "the lone repo-aware seat is not enough; the multi-seat gate is load-bearing"
  ],
  "follow_up_beads": [
    "etude-jb7 symlink-path-traversal residual (operator authors the spec)",
    "etude dogfood-capture.sh rewire onto capture-run"
  ],
  "decisions": [],
  "durable_changes": [
    "review-gate-runbook.md \"Recurring Defect Classes\" item 4: an output-membership assertion must match X at its exact rendered slot (whole token + indent/position), never substring/Contains"
  ]
}
