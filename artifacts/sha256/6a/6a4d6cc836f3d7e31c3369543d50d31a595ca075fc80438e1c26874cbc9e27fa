# etude-5dr.6 final review round 3

Final commit `acce143` resolves the round-two review cases: intent-to-add is pinned by an explicit pre-run regression against the existing cached-index rejection; non-empty pre-existing graft metadata is unsupported; forged completed caller lineage is rejected before runner invocation; and raw untracked discovery/classification forces `core.ignoreCase=false`. Full verification and fresh built-binary QA are rerunning on this exact code.
