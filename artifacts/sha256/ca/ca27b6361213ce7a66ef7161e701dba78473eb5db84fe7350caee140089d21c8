# etude-5dr.6 documentation round 2

`docs/run.md` documents the opt-in `runner.workspace: caller` contract, unchanged hermetic default and gates, manifest v4 original-versus-stage provenance, pre/post fail-closed guard, tracked byte/mode and Git-control inspection, history ancestry, crash/resume cleanup, the intentional ignored-artifact boundary, and the explicit fact that Etude records but does not remotely publish the runner-created commit.
