The docs contradict failure behavior by implying any non-empty stream produces a durable log even though only successful stages have Stage records. The sidecar example also supplies divergent URI/path values without explaining that URI is opaque metadata while path controls best-effort import and path resolution.

VERDICT: BLOCK
