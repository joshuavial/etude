Proportionality, trust boundary, and blast radius are sound.

BLOCK: input `python. -c script` produces `opaqueWrapper=true`, although
`python.` has no numeric version and may be the configured reviewer executable.
Require numeric dot-separated components.

VERDICT: BLOCK
