packet: .etude/tmp/etude-s6z.1/plan.packet.md
sha256: ff8b5d8622f6b478fc7b52286dc6374d39a6f7eb710ff36b9c59b0f46d426d04

Proportionality: The P2 ancestor needs a real Claude producer adapter; existing ExecRunner and reviewer registry cannot provide that contract. The measured output-missing failure justifies the small wrapper.

Blocking defects:
1. Require non-empty temporary Claude output before rename and test successful empty output.
2. Sending the remote request is irreversible and can be charged again after a crash before publication; declare the residual.
3. The dirty-tree proof writes temporary objects to the shared Git object database; include that in the blast radius.

VERDICT: BLOCK
