# Final Review — etude-kig (extract shared retro subject-validation + ref-parsing helpers)
Verdict: GO (3-seat UNANIMOUS: codex + gemini + Opus). Commit f744af8.
- Closes the last review refactor finding, premise-checked + NARROWED: the audit implied a ~50-line preamble extraction, but a wide {scope+subjects+gitsha+refs} helper would REORDER capture's interleaved '--file required' (between scope and subjects) and parseRefs (after body/meta reads) -> changing error precedence (a behavior change). Narrowed to the two CONTIGUOUS, byte-identical, same-relative-position blocks: validateSubjectsResolveSHA {subject-presence + IsValidRunID + git-sha resolve} and parseAndCheckRefs {parseRefs + checkReservedRefs}, each replacing its block in place so the order of checks is preserved exactly in both runners.
- Behavior-identical: every error string + the order of checks unchanged (verified by all 3 seats against HEAD); the inline blocks for path-specific checks (capture --file/body/meta; generate generator/materialize) + scope-validate stay inline. Added an invalid-`--subject-run` test (codex coverage-gap note). Full go test ./... green.
- Gate: Tier-2 PLAN gate GO (codex + gemini, both confirmed the order-preservation analysis + correctly endorsed rejecting the wide extraction). IMPLEMENT gate 3-seat UNANIMOUS GO (codex + gemini + Opus; Opus did the HEAD-vs-working diff order-check). codex-exec cooperated at default effort with tight payloads (xhigh + large diffs were hanging this session-segment — a transient codex-exec outage).
- Scope: retro.go + retro_test.go. 1-bead=1-commit. No docs.
