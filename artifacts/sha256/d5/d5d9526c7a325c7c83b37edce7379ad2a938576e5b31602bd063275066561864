VERDICT: BLOCK
BLOCKING: 1. R1 requires rejection before scratch creation, but the supplied runGateCommand calls os.MkdirTemp before GateStage performs the proposed binding check. Include a CLI change that defers scratch creation until binding succeeds, and CLI-level verification that mismatch creates no scratch directory.
OPTIONAL: 1. Strengthen R2’s test by asserting every seat receives the expected artifact bytes in its prompt, alongside the recorded digest assertion.
CONFIDENCE: high
