# etude-5dr.6 implementation

Commit `3dcf0a2` adds authoritative caller provenance checks: `HEAD^{commit}`, original-A and prior-caller ancestry with replacement objects disabled, byte snapshots of `info/exclude` and `info/grafts`, and raw untracked-path stability independent of ignore rules. Seven focused regressions exercise clean rewrite, graft file, replacement graft, forged prior caller lineage, info/exclude, external excludes, and tree-valued HEAD; each proves no new stage/ref is published.
