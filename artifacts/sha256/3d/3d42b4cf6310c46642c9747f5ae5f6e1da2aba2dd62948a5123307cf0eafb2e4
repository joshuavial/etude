The built proof is strong, but replay recording persists RunResult.Log and ignores RunResult.Session, so a successful external replay runner loses its session identity in the replay manifest.

VERDICT: BLOCK
