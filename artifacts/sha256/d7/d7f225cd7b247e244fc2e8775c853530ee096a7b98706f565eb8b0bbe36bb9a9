commit 1c28486b550ddcb318d608daf663b7261f595270
Author: joshuavial <git@codewithjv.com>
Date:   Mon May 25 22:27:17 2026 +0800

    add generated cli reference docs and gen-docs tool

diff --git a/Makefile b/Makefile
index 4ae4ef9..7aa6271 100644
--- a/Makefile
+++ b/Makefile
@@ -1,8 +1,9 @@
 BINARY := etude
 BIN_DIR := bin
 VERSION ?= dev
+DOCS_DIR := docs/cli
 
-.PHONY: build test lint clean
+.PHONY: build test lint clean docs docs-check
 
 build:
 	mkdir -p $(BIN_DIR)
@@ -17,3 +18,10 @@ lint:
 
 clean:
 	rm -rf $(BIN_DIR)
+
+docs:
+	go run ./cmd/gen-docs -out $(DOCS_DIR)
+
+docs-check:
+	@TMP=$$(mktemp -d); trap 'rm -rf "$$TMP"' EXIT; \
+		go run ./cmd/gen-docs -out "$$TMP" && diff -r "$$TMP" $(DOCS_DIR)
diff --git a/cmd/gen-docs/main.go b/cmd/gen-docs/main.go
new file mode 100644
index 0000000..7deb2d2
--- /dev/null
+++ b/cmd/gen-docs/main.go
@@ -0,0 +1,100 @@
+// Command gen-docs generates Markdown reference documentation for all etude CLI
+// commands and writes them into the output directory.
+//
+// Usage:
+//
+//	go run ./cmd/gen-docs [-out <dir>]
+//
+// The output directory is cleaned of existing *.md files before generation so
+// that stale pages from removed commands do not accumulate.
+package main
+
+import (
+	"flag"
+	"fmt"
+	"io"
+	"os"
+	"path/filepath"
+
+	"github.com/joshuavial/etude/internal/cli"
+	"github.com/spf13/cobra"
+	"github.com/spf13/cobra/doc"
+)
+
+func main() {
+	outDir := flag.String("out", "docs/cli", "directory to write generated docs into")
+	flag.Parse()
+
+	if err := run(*outDir); err != nil {
+		fmt.Fprintln(os.Stderr, "gen-docs:", err)
+		os.Exit(1)
+	}
+}
+
+func run(outDir string) error {
+	if err := os.MkdirAll(outDir, 0o755); err != nil {
+		return fmt.Errorf("create output dir: %w", err)
+	}
+
+	if err := cleanMarkdownFiles(outDir); err != nil {
+		return fmt.Errorf("clean output dir: %w", err)
+	}
+
+	root := cli.NewRootCommand(io.Discard, io.Discard)
+	disableAutoGenTag(root)
+
+	filePrepender := func(string) string {
+		return "<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->\n\n"
+	}
+	// linkHandler returns the filename unchanged — links stay relative within docs/cli/.
+	linkHandler := func(s string) string { return s }
+
+	if err := doc.GenMarkdownTreeCustom(root, outDir, filePrepender, linkHandler); err != nil {
+		return fmt.Errorf("generate docs: %w", err)
+	}
+
+	n, err := countMarkdownFiles(outDir)
+	if err != nil {
+		return err
+	}
+	fmt.Printf("generated %d command docs into %s\n", n, outDir)
+	return nil
+}
+
+// disableAutoGenTag walks the entire command tree (root and all descendants)
+// and sets DisableAutoGenTag = true on each command so cobra never appends a
+// dated "Auto generated by spf13/cobra on <date>" footer.
+func disableAutoGenTag(cmd *cobra.Command) {
+	cmd.DisableAutoGenTag = true
+	for _, sub := range cmd.Commands() {
+		disableAutoGenTag(sub)
+	}
+}
+
+// cleanMarkdownFiles removes only GENERATED markdown files (those named
+// "etude*.md" — the root command is "etude", so every generated page matches
+// that prefix) without removing the directory itself. Scoping the glob to the
+// generated prefix means that even if --out is mistakenly pointed at a directory
+// containing hand-written docs (e.g. docs/, which holds run.md, bench.md,
+// README.md — none of which start with "etude"), this never deletes them.
+func cleanMarkdownFiles(dir string) error {
+	entries, err := filepath.Glob(filepath.Join(dir, "etude*.md"))
+	if err != nil {
+		return err
+	}
+	for _, path := range entries {
+		if err := os.Remove(path); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+// countMarkdownFiles returns the number of generated "etude*.md" files in dir.
+func countMarkdownFiles(dir string) (int, error) {
+	entries, err := filepath.Glob(filepath.Join(dir, "etude*.md"))
+	if err != nil {
+		return 0, err
+	}
+	return len(entries), nil
+}
diff --git a/cmd/gen-docs/main_test.go b/cmd/gen-docs/main_test.go
new file mode 100644
index 0000000..216f2a6
--- /dev/null
+++ b/cmd/gen-docs/main_test.go
@@ -0,0 +1,171 @@
+package main
+
+import (
+	"bytes"
+	"os"
+	"path/filepath"
+	"strings"
+	"testing"
+)
+
+// expectedCommands lists every command the root tree should document.
+// One file per entry, named <command path with spaces replaced by underscores>.md.
+var expectedCommands = []string{
+	"etude.md",
+	"etude_bench.md",
+	"etude_capture.md",
+	"etude_gc.md",
+	"etude_init.md",
+	"etude_reindex.md",
+	"etude_replay.md",
+	"etude_run.md",
+	"etude_run_list.md",
+	"etude_run_show.md",
+	"etude_sync.md",
+}
+
+func TestGeneratesOneFilePerCommand(t *testing.T) {
+	dir := t.TempDir()
+	if err := run(dir); err != nil {
+		t.Fatalf("run: %v", err)
+	}
+
+	for _, name := range expectedCommands {
+		path := filepath.Join(dir, name)
+		if _, err := os.Stat(path); err != nil {
+			t.Errorf("missing expected file: %s", name)
+		}
+	}
+}
+
+func TestFilesContainSynopsisAndFlags(t *testing.T) {
+	dir := t.TempDir()
+	if err := run(dir); err != nil {
+		t.Fatalf("run: %v", err)
+	}
+
+	checks := map[string][]string{
+		"etude.md":          {"## etude", "etude"},
+		"etude_bench.md":    {"## etude bench", "bench"},
+		"etude_run.md":      {"## etude run", "run"},
+		"etude_run_list.md": {"## etude run list", "list"},
+		"etude_run_show.md": {"## etude run show", "show"},
+		"etude_init.md":     {"## etude init", "init"},
+		"etude_capture.md":  {"## etude capture", "capture"},
+		"etude_gc.md":       {"## etude gc", "gc"},
+		"etude_replay.md":   {"## etude replay", "replay"},
+		"etude_sync.md":     {"## etude sync", "sync"},
+		"etude_reindex.md":  {"## etude reindex", "reindex"},
+	}
+
+	for file, terms := range checks {
+		content, err := os.ReadFile(filepath.Join(dir, file))
+		if err != nil {
+			t.Errorf("cannot read %s: %v", file, err)
+			continue
+		}
+		for _, term := range terms {
+			if !strings.Contains(string(content), term) {
+				t.Errorf("%s: expected to contain %q", file, term)
+			}
+		}
+	}
+}
+
+func TestNoAutoGeneratedDateFooter(t *testing.T) {
+	dir := t.TempDir()
+	if err := run(dir); err != nil {
+		t.Fatalf("run: %v", err)
+	}
+
+	entries, _ := filepath.Glob(filepath.Join(dir, "*.md"))
+	for _, path := range entries {
+		content, err := os.ReadFile(path)
+		if err != nil {
+			t.Fatalf("read %s: %v", path, err)
+		}
+		if strings.Contains(string(content), "Auto generated") {
+			t.Errorf("%s contains 'Auto generated' footer — output is not deterministic", filepath.Base(path))
+		}
+	}
+}
+
+func TestDeterministicOutput(t *testing.T) {
+	dir1 := t.TempDir()
+	dir2 := t.TempDir()
+
+	if err := run(dir1); err != nil {
+		t.Fatalf("first run: %v", err)
+	}
+	if err := run(dir2); err != nil {
+		t.Fatalf("second run: %v", err)
+	}
+
+	for _, name := range expectedCommands {
+		a, err := os.ReadFile(filepath.Join(dir1, name))
+		if err != nil {
+			t.Errorf("missing from first run: %s", name)
+			continue
+		}
+		b, err := os.ReadFile(filepath.Join(dir2, name))
+		if err != nil {
+			t.Errorf("missing from second run: %s", name)
+			continue
+		}
+		if !bytes.Equal(a, b) {
+			t.Errorf("%s differs between two runs — output is not deterministic", name)
+		}
+	}
+}
+
+func TestDriftGuard(t *testing.T) {
+	// Locate the committed docs/cli directory relative to this file's module root.
+	// We walk up from the test binary's working directory to find go.mod.
+	committedDir, err := findCommittedCliDir()
+	if err != nil {
+		t.Skip("cannot locate committed docs/cli:", err)
+	}
+
+	dir := t.TempDir()
+	if err := run(dir); err != nil {
+		t.Fatalf("run: %v", err)
+	}
+
+	// Compare every expected file.
+	for _, name := range expectedCommands {
+		committed, err := os.ReadFile(filepath.Join(committedDir, name))
+		if err != nil {
+			t.Errorf("committed docs/cli/%s missing — run `make docs` to regenerate", name)
+			continue
+		}
+		generated, err := os.ReadFile(filepath.Join(dir, name))
+		if err != nil {
+			t.Errorf("generated docs missing %s", name)
+			continue
+		}
+		if !bytes.Equal(committed, generated) {
+			t.Errorf("docs/cli/%s is stale — run `make docs` to regenerate", name)
+		}
+	}
+}
+
+// findCommittedCliDir resolves the docs/cli path by walking up from the
+// test's working directory to find go.mod, then appending docs/cli.
+func findCommittedCliDir() (string, error) {
+	wd, err := os.Getwd()
+	if err != nil {
+		return "", err
+	}
+	dir := wd
+	for {
+		if _, err := os.Stat(filepath.Join(dir, "go.mod")); err == nil {
+			return filepath.Join(dir, "docs", "cli"), nil
+		}
+		parent := filepath.Dir(dir)
+		if parent == dir {
+			break
+		}
+		dir = parent
+	}
+	return "", os.ErrNotExist
+}
diff --git a/docs/README.md b/docs/README.md
index 2adb4e1..4699384 100644
--- a/docs/README.md
+++ b/docs/README.md
@@ -13,6 +13,7 @@ This directory holds user-facing documentation for implemented behavior.
 - [GC](gc.md) - report artifact storage and explicitly prune named run refs.
 - [Reindex](reindex.md) - build the derived SQLite query index from all run and eval refs.
 - [Plans](plans/README.md) - notes on planned components that do not exist yet.
+- [CLI reference](cli/etude.md) - generated per-command flag/synopsis reference (do not edit; run `make docs` to regenerate).
 
 The current implemented state is summarized in the top-level
 [README](../README.md).
diff --git a/docs/cli/etude.md b/docs/cli/etude.md
new file mode 100644
index 0000000..4ccce26
--- /dev/null
+++ b/docs/cli/etude.md
@@ -0,0 +1,31 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude
+
+Root CLI scaffold for etude
+
+### Synopsis
+
+etude captures stage artifacts as git-native run records. The replay command re-executes a recorded stage end-to-end. The bench command benchmarks a cohort of runs by replaying and judging replay vs original.
+
+```
+etude [flags]
+```
+
+### Options
+
+```
+  -h, --help   help for etude
+```
+
+### SEE ALSO
+
+* [etude bench](etude_bench.md)	 - Benchmark a stage by replaying the cohort and judging replay vs original
+* [etude capture](etude_capture.md)	 - Capture a stage artifact into an etude run
+* [etude gc](etude_gc.md)	 - Report artifact storage or prune named run refs
+* [etude init](etude_init.md)	 - Scaffold .etude/ config and register refs/etude/* refspecs
+* [etude reindex](etude_reindex.md)	 - Rebuild the SQLite query index from all run and eval refs
+* [etude replay](etude_replay.md)	 - Replay a recorded stage end-to-end
+* [etude run](etude_run.md)	 - Inspect etude runs
+* [etude sync](etude_sync.md)	 - Push and fetch refs/etude/* with a git remote
+
diff --git a/docs/cli/etude_bench.md b/docs/cli/etude_bench.md
new file mode 100644
index 0000000..129530f
--- /dev/null
+++ b/docs/cli/etude_bench.md
@@ -0,0 +1,32 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude bench
+
+Benchmark a stage by replaying the cohort and judging replay vs original
+
+```
+etude bench <stage> [flags]
+```
+
+### Options
+
+```
+      --harness string           override harness name in recorded producer (contestant)
+      --harness-version string   override harness version in recorded producer (contestant)
+  -h, --help                     help for bench
+      --judge string             judge command spec (e.g. ./judge.sh)
+      --judge-model string       model passed to the judge as ETUDE_MODEL (falls back to git config etude.judgeModel; empty is allowed)
+      --last int                 number of most-recent qualifying runs to benchmark (must be >0) (default 10)
+      --model string             override model in recorded producer (contestant, NOT the judge/referee — use --judge-model for that)
+      --no-cache                 force re-evaluation; skip the eval-result cache
+      --runner string            runner command spec (e.g. ./run.sh)
+      --seed int                 seed for per-pair presentation randomisation
+      --skill-id string          override skill id in recorded producer (contestant)
+      --skill-repo string        override skill repo in recorded producer (contestant)
+      --skill-version string     override skill version in recorded producer (contestant)
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/docs/cli/etude_capture.md b/docs/cli/etude_capture.md
new file mode 100644
index 0000000..8e3373f
--- /dev/null
+++ b/docs/cli/etude_capture.md
@@ -0,0 +1,35 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude capture
+
+Capture a stage artifact into an etude run
+
+```
+etude capture <stage> [flags]
+```
+
+### Options
+
+```
+      --git-sha string            repo git sha for this stage
+      --harness string            agent runtime that executed the stage (e.g. claude-code)
+      --harness-version string    version of the agent runtime
+  -h, --help                      help for capture
+      --input stringArray         input artifact as role=path
+      --message string            run ref commit message
+      --model string              LLM model used by this stage (e.g. claude-opus-4-7)
+      --output stringArray        output artifact as role=path
+      --produced-by string        producer identity (default "original")
+      --ref stringArray           external ref as key=value
+      --run string                run id to capture into
+      --skill-id string           skill id
+      --skill-repo string         skill repo (default "manual")
+      --skill-version string      skill version (default "manual")
+      --workflow string           workflow name (default "manual")
+      --workflow-version string   workflow version (default "manual-v1")
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/docs/cli/etude_gc.md b/docs/cli/etude_gc.md
new file mode 100644
index 0000000..7eacae5
--- /dev/null
+++ b/docs/cli/etude_gc.md
@@ -0,0 +1,22 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude gc
+
+Report artifact storage or prune named run refs
+
+```
+etude gc [--max-size N] [--prune] [run-id...] [flags]
+```
+
+### Options
+
+```
+  -h, --help           help for gc
+      --max-size int   report runs whose total content-artifact bytes exceed N
+      --prune          delete named run refs (requires run-id args)
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/docs/cli/etude_init.md b/docs/cli/etude_init.md
new file mode 100644
index 0000000..501de07
--- /dev/null
+++ b/docs/cli/etude_init.md
@@ -0,0 +1,22 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude init
+
+Scaffold .etude/ config and register refs/etude/* refspecs
+
+```
+etude init [flags]
+```
+
+### Options
+
+```
+      --force           overwrite existing scaffolded files with fresh generated content
+  -h, --help            help for init
+      --remote string   git remote to configure refspecs on (default: origin) (default "origin")
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/docs/cli/etude_reindex.md b/docs/cli/etude_reindex.md
new file mode 100644
index 0000000..35d378e
--- /dev/null
+++ b/docs/cli/etude_reindex.md
@@ -0,0 +1,20 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude reindex
+
+Rebuild the SQLite query index from all run and eval refs
+
+```
+etude reindex [flags]
+```
+
+### Options
+
+```
+  -h, --help   help for reindex
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/docs/cli/etude_replay.md b/docs/cli/etude_replay.md
new file mode 100644
index 0000000..f6d3c41
--- /dev/null
+++ b/docs/cli/etude_replay.md
@@ -0,0 +1,29 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude replay
+
+Replay a recorded stage end-to-end
+
+```
+etude replay <run> <stage> [flags]
+```
+
+### Options
+
+```
+      --harness string           override harness name in recorded producer
+      --harness-version string   override harness version in recorded producer
+  -h, --help                     help for replay
+      --model string             override model in recorded producer
+      --output string            write output to this file instead of stdout
+      --record                   persist the replay output as a new linked run
+      --runner string            runner command spec (e.g. ./run.sh)
+      --skill-id string          override skill id in recorded producer
+      --skill-repo string        override skill repo in recorded producer
+      --skill-version string     override skill version in recorded producer
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/docs/cli/etude_run.md b/docs/cli/etude_run.md
new file mode 100644
index 0000000..bd06660
--- /dev/null
+++ b/docs/cli/etude_run.md
@@ -0,0 +1,22 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude run
+
+Inspect etude runs
+
+```
+etude run [flags]
+```
+
+### Options
+
+```
+  -h, --help   help for run
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+* [etude run list](etude_run_list.md)	 - List all runs
+* [etude run show](etude_run_show.md)	 - Show details of a run
+
diff --git a/docs/cli/etude_run_list.md b/docs/cli/etude_run_list.md
new file mode 100644
index 0000000..0bb0134
--- /dev/null
+++ b/docs/cli/etude_run_list.md
@@ -0,0 +1,20 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude run list
+
+List all runs
+
+```
+etude run list [flags]
+```
+
+### Options
+
+```
+  -h, --help   help for list
+```
+
+### SEE ALSO
+
+* [etude run](etude_run.md)	 - Inspect etude runs
+
diff --git a/docs/cli/etude_run_show.md b/docs/cli/etude_run_show.md
new file mode 100644
index 0000000..c20ba90
--- /dev/null
+++ b/docs/cli/etude_run_show.md
@@ -0,0 +1,20 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude run show
+
+Show details of a run
+
+```
+etude run show <run-id> [flags]
+```
+
+### Options
+
+```
+  -h, --help   help for show
+```
+
+### SEE ALSO
+
+* [etude run](etude_run.md)	 - Inspect etude runs
+
diff --git a/docs/cli/etude_sync.md b/docs/cli/etude_sync.md
new file mode 100644
index 0000000..ccf08ff
--- /dev/null
+++ b/docs/cli/etude_sync.md
@@ -0,0 +1,21 @@
+<!-- Code generated by gen-docs; DO NOT EDIT. Run `make docs` to regenerate. -->
+
+## etude sync
+
+Push and fetch refs/etude/* with a git remote
+
+```
+etude sync [flags]
+```
+
+### Options
+
+```
+  -h, --help            help for sync
+      --remote string   git remote to sync with (default: origin) (default "origin")
+```
+
+### SEE ALSO
+
+* [etude](etude.md)	 - Root CLI scaffold for etude
+
diff --git a/go.mod b/go.mod
index 1cde036..a7dcbb5 100644
--- a/go.mod
+++ b/go.mod
@@ -9,13 +9,16 @@ require (
 )
 
 require (
+	github.com/cpuguy83/go-md2man/v2 v2.0.6 // indirect
 	github.com/dustin/go-humanize v1.0.1 // indirect
 	github.com/google/uuid v1.6.0 // indirect
 	github.com/inconshreveable/mousetrap v1.1.0 // indirect
 	github.com/mattn/go-isatty v0.0.20 // indirect
 	github.com/ncruces/go-strftime v1.0.0 // indirect
 	github.com/remyoudompheng/bigfft v0.0.0-20230129092748-24d4a6f8daec // indirect
+	github.com/russross/blackfriday/v2 v2.1.0 // indirect
 	github.com/spf13/pflag v1.0.9 // indirect
+	go.yaml.in/yaml/v3 v3.0.4 // indirect
 	golang.org/x/sys v0.42.0 // indirect
 	modernc.org/libc v1.72.3 // indirect
 	modernc.org/mathutil v1.7.1 // indirect
diff --git a/go.sum b/go.sum
index 6969445..371cdb3 100644
--- a/go.sum
+++ b/go.sum
@@ -1,3 +1,4 @@
+github.com/cpuguy83/go-md2man/v2 v2.0.6 h1:XJtiaUW6dEEqVuZiMTn1ldk455QWwEIsMIJlo5vtkx0=
 github.com/cpuguy83/go-md2man/v2 v2.0.6/go.mod h1:oOW0eioCTA6cOiMLiUPZOpcVxMig6NIQQ7OS05n1F4g=
 github.com/dustin/go-humanize v1.0.1 h1:GzkhY7T5VNhEkwH0PVJgjz+fX1rhBrR7pRT3mDkpeCY=
 github.com/dustin/go-humanize v1.0.1/go.mod h1:Mu1zIs6XwVuF/gI1OepvI0qD18qycQx+mFykh5fBlto=
@@ -15,11 +16,13 @@ github.com/ncruces/go-strftime v1.0.0 h1:HMFp8mLCTPp341M/ZnA4qaf7ZlsbTc+miZjCLOF
 github.com/ncruces/go-strftime v1.0.0/go.mod h1:Fwc5htZGVVkseilnfgOVb9mKy6w1naJmn9CehxcKcls=
 github.com/remyoudompheng/bigfft v0.0.0-20230129092748-24d4a6f8daec h1:W09IVJc94icq4NjY3clb7Lk8O1qJ8BdBEF8z0ibU0rE=
 github.com/remyoudompheng/bigfft v0.0.0-20230129092748-24d4a6f8daec/go.mod h1:qqbHyh8v60DhA7CoWK5oRCqLrMHRGoxYCSS9EjAz6Eo=
+github.com/russross/blackfriday/v2 v2.1.0 h1:JIOH55/0cWyOuilr9/qlrm0BSXldqnqwMsf35Ld67mk=
 github.com/russross/blackfriday/v2 v2.1.0/go.mod h1:+Rmxgy9KzJVeS9/2gXHxylqXiyQDYRxCVz55jmeOWTM=
 github.com/spf13/cobra v1.10.2 h1:DMTTonx5m65Ic0GOoRY2c16WCbHxOOw6xxezuLaBpcU=
 github.com/spf13/cobra v1.10.2/go.mod h1:7C1pvHqHw5A4vrJfjNwvOdzYu0Gml16OCs2GRiTUUS4=
 github.com/spf13/pflag v1.0.9 h1:9exaQaMOCwffKiiiYk6/BndUBv+iRViNW+4lEMi0PvY=
 github.com/spf13/pflag v1.0.9/go.mod h1:McXfInJRrz4CZXVZOBLb0bTZqETkiAhM9Iw0y3An2Bg=
+go.yaml.in/yaml/v3 v3.0.4 h1:tfq32ie2Jv2UxXFdLJdh3jXuOzWiL1fo0bu/FbuKpbc=
 go.yaml.in/yaml/v3 v3.0.4/go.mod h1:DhzuOOF2ATzADvBadXxruRBLzYTpT36CKvDb3+aBEFg=
 golang.org/x/mod v0.33.0 h1:tHFzIWbBifEmbwtGz65eaWyGiGZatSrT9prnU8DbVL8=
 golang.org/x/mod v0.33.0/go.mod h1:swjeQEj+6r7fODbD2cqrnje9PnziFuw4bmLbBZFrQ5w=
