✓ etude-8t4 · etude retro list + show (read side over refs/etude/retros)   [● P2 · CLOSED]
Owner: joshuavial · Type: task
Created: 2026-05-25 · Updated: 2026-05-25
Close reason: implemented etude retro list + show, Tier-2 gate passed (2 rounds), cdacae4

DESCRIPTION
Phase B slice 2 of etude-14r (design: etude-retro-command.md §2 Phase B). Add 'etude retro list' + 'etude retro show <id>' over the refs/etude/retros/* namespace created in etude-q87. The run-list walk is bound to the runs namespace, so add a retros-namespace walk. 'retro show' wants a body dump that 'run show' lacks — factor the shared header/stage rendering (printRunDetail) rather than forking it (design open-issue). Render the retro's subjects (indexed subject_run.* Refs) + findings/changes body.

DESIGN
## Plan

**Approach:** Add two read-only subcommands — `etude retro list` and `etude retro show <id>` — to the existing `retro` parent command, modelled directly on `etude run list`/`run show` (internal/cli/run.go). They walk the already-shipped, already-allowlisted `refs/etude/retros/*` namespace via refstore.List/Resolve/ReadFile and parse the same runmanifest.Manifest the q87 writer produces (Workflow=retro, one `retro` stage, role:retro markdown Output, indexed subject_run.N/bead.N + flat scope/trigger/decision/supersedes/gate.1/bench.1/eval.1 Refs). The one deliberate divergence from `run show`: `retro show` DUMPS the markdown body inline (the body IS the retro), which `run show` never does (it prints only artifact-path metadata) — design §2 Phase B says retro show 'wants a body dump that run show lacks'. No new schema, no new namespace, no writer/mutation, no generate/triggers/config (that is etude-n0t, deferred).

**Files:**
- internal/cli/retro.go - MODIFY: register the two new subcommands on the existing newRetroCommand parent (cmd.AddCommand(newRetroListCommand(...)), newRetroShowCommand(...)); add a retroShowListRunner{store, stdout} mirroring runShowListRunner. Keep it in this file (q87 keeps capture here; run.go keeps run list+show+printRunDetail together — one file per parent command is the repo convention).
- internal/cli/retro.go - ADD: retroListRunner.list() — refstore.List over retrosPrefix (refs/etude/retros), TrimPrefix each ref, ReadFile manifest.json, ParseJSON, render a tabwriter table. Header: RETRO ID | SCOPE | TRIGGER | SUBJECTS | CREATED (mirrors design §2 Phase B columns). SCOPE/TRIGGER from Refs[scope]/Refs[trigger]; SUBJECTS = count or comma-join of indexed subject_run.N + bead.N (sorted by index); CREATED = manifest.Created RFC3339 UTC. Empty -> 'no retros found', exit 0 (mirror 'no runs found'). Corrupt/missing manifest -> error naming the retro id, nothing to stdout (mirror run list).
- internal/cli/retro.go - ADD: retroShowRunner.show(id) — validateCLIIdentifier('retro id', id) + retro.IsValidRetroID(id) guard (mirror validateRunIDExtra); Resolve refs/etude/retros/<id>, map refstore.ErrNotFound -> 'retro "<id>" not found'; ReadFile+ParseJSON manifest; render header (retro id/scope/trigger/decision/supersedes/created), sorted subjects (subject_run.N then bead.N), gate/bench/eval refs, producer (harness/model/skill from the single retro stage), then read the Output body via store.ReadFile(ref, stage.Output.Path) and print it inline under a '--- retro body ---' divider. Storage is always content (capture uses AddContent), so a direct ReadFile dump is correct; guard non-content storage by printing the artifact path instead.
- internal/cli/retro_test.go - MODIFY: add TestRetroList* (zero/one/many+lexical order/corrupt-manifest) and TestRetroShow* (existing happy path incl. body dump + subjects + scope/trigger/decision, unknown id, invalid id before git) mirroring run_test.go. Reuse existing helpers initCaptureRepo/writeFile/chdir/execute and the existing retro capture path to seed retros.
- docs/cli/etude_retro_list.md, docs/cli/etude_retro_show.md - GENERATED by `make docs` (gen-docs auto-walks the command tree). Must be committed.
- cmd/gen-docs/main_test.go - MODIFY: add 'etude_retro_list.md' and 'etude_retro_show.md' to expectedCommands (and, since q87 left them out, optionally 'etude_retro.md'/'etude_retro_capture.md' — but keep 1-bead scope: add at minimum the two new pages). Add a checks entry for the two new pages.
- docs/run.md is the model for a hand-written user doc, but README.md already advertises `retro` (top-level) and docs/README.md already indexes it at cli/etude_retro.md, so docs-reality-check already passes for the parent. A dedicated hand-written docs/retro.md is OPTIONAL polish, not required by the docs gate; recommend updating the existing docs/README.md Retro line to mention list/show, plus a short list/show section if a hand doc is wanted. Decide during Docs phase.

**Tests:**
- retro list: zero retros -> 'no retros found' + exit 0; one retro -> header columns present + a data row with id/scope/trigger/subject-count/RFC3339-Z created; multiple -> lexical order by id; corrupt manifest -> non-zero, names id, nothing on stdout.
- retro show: existing retro renders id/scope/trigger/decision/created + indexed subjects in order + the inline markdown body bytes (round-trips capture); unknown id -> 'retro "<id>" not found' non-zero; invalid id (slash/.. /.lock) rejected before any git call.
- Round-trip: seed via `retro capture` in the test repo, then list+show, asserting the read side reproduces exactly what the writer stored (Workflow/stage/Output role + Refs).
- Edge cases: workflow-scope retro (no subject_run) lists with empty/zero SUBJECTS; multi-subject cohort shows all subject_run.N + bead.N sorted; --ref custom keys do not break rendering.

**Docs:**
- REQUIRED: regenerate docs/cli via `make docs` (adds etude_retro_list.md + etude_retro_show.md) and extend cmd/gen-docs/main_test.go expectedCommands. Run `make docs-check` and `make docs-reality` — both must stay green.
- OPTIONAL: extend docs/README.md Retro index line / add a docs/retro.md list+show section mirroring docs/run.md. Not gate-required since `retro` is already advertised; decide in Docs phase.

**Risks:** Low. Read-only, mirrors run list/show over an already-shipped+allowlisted namespace; no schema/namespace/writer changes. Only subtlety is the deliberate inline body dump (the one place retro show diverges from run show) and refactoring shared header/stage rendering: per design open-issue, FACTOR shared rendering out of printRunDetail rather than forking it — but keep that refactor minimal and within run.go/retro.go to stay 1-commit; if factoring printRunDetail risks touching run show output/tests, prefer a small retro-local renderer and note the shared-rendering follow-up. Generated-doc + gen-docs test sync is a known checklist item, not a risk.

**Gate tier:** Tier 2 (Opus + codex). Read-only product CLI modeled directly on run list/run show over an already-shipped namespace; no new schema, namespace, or mutation. The only judgement calls (inline body dump; whether to refactor printRunDetail vs a local renderer) are local and low-blast-radius — they do not rise to Tier 1.

**Capture:**
- inputs: bead etude-8t4 text; docs/plans/product/etude-retro-command.md (§2 Phase B read side); internal/cli/run.go (run list/show + printRunDetail); internal/cli/retro.go + internal/retro/retro.go (q87 write shape); internal/refstore/store.go (List/Resolve/ReadFile + retrosNS allowlist); internal/artifactstore/store.go (content body); internal/cli/run_test.go + retro_test.go (test harness); cmd/gen-docs + Makefile + scripts/docs-reality-check.sh + docs/run.md + docs/README.md (docs gate).
- output: bead --design field


NOTES
## Docs

Changed: yes
Files:
- docs/cli/etude_retro_list.md (new, generated)
- docs/cli/etude_retro_show.md (new, generated)
- docs/cli/etude_retro.md (updated, generated — SEE ALSO now lists list + show)
- cmd/gen-docs/main_test.go (added etude_retro_list.md + etude_retro_show.md to expectedCommands)
- docs/retro.md (new, hand-written user guide: capture + list + show)
- docs/README.md (updated — retro link now points to retro.md instead of cli/etude_retro.md)

Rationale:
- Generated CLI reference regenerated via `make docs`; etude_retro_list.md and etude_retro_show.md are new pages; etude_retro.md SEE ALSO updated to include the two new subcommands
- Two new pages added to expectedCommands in gen-docs test (scope-limited per bead instructions; known incomplete list tracked separately)
- docs/retro.md added: retro command family now has all three sides shipped (capture + list + show), matching the established docs/run.md pattern for a command family; the docs/README.md link updated from the generated reference to the hand-written guide
- docs-reality stayed green; the new subcommands are not top-level so the reality guard does not require top-level index entries for them

Policy check:
- policy applied: repo convention — generated docs/cli/* via `make docs`; hand-written guides in docs/<cmd>.md; docs/README.md as the link index
- shipped docs describe implemented behavior only (capture + list + show; no generate — that is deferred)
- planning material remains under docs/plans/
- generated references (docs/cli/*) are separate from hand-written guides (docs/retro.md)
- generated output was updated through `make docs` workflow

Capture:
- inputs: Verify artifact and docs policy
- output: docs diff — 3 generated cli files updated/added, 2 hand-written files updated/added, gen-docs test extended

DEPENDS ON
  → ○ etude-14r: Capture retros and session activity as a first-class etude artifact ● P0
  → ✓ etude-q87: Retro storage + etude retro capture (refs/etude/retros namespace + writer) ● P0

BLOCKS
  ← ○ etude-14r: Capture retros and session activity as a first-class etude artifact ● P0

COMMENTS
  2026-05-25 19:54 joshuavial
    ## Verify
    
    Status: pass
    Recommendation: proceed to Docs
    
    Automated tests:
    - changes: none added in Verify (dev-test-writer not spawned; comprehensive tests already present in internal/cli/retro_test.go from Implement — 13 retro test groups covering list zero/one/many/lexical-order/corrupt-manifest and show happy-path/body-dump/subjects/flat-metadata/unknown-id/invalid-id-before-git[6 subcases]/workflow-no-subjects). Coverage judged adequate; no Verify-only test gaps found.
    - commands: `go build ./...`; `go test ./...`; `make lint`; `go test ./internal/cli/ -run Retro -count=1 -v`; `make build`; built-binary manual test in throwaway git repos
    - result: PASS. Build clean. `go test ./...` all green (all 15 packages ok). `make lint` clean (gofmt + go vet). Uncached retro CLI tests: all 13 groups PASS.
    
    Manual tests:
    - required: yes
    - reason: CLI bead — built-binary behavior is the critical verification (per dev-workflow expectations). Output formatting, table layout, byte-for-byte body rendering, and error/exit-code paths must be observed in the real binary.
    - result: PASS. Built bin/etude via `make build`. In a throwaway repo (git init + etude init + seed commit) captured 3 retros via the shipped `retro capture`: (a) cohort with 2 subject-runs + 2 beads + trigger/decision, (b) gate with --gate/--bench/--eval/--ref note=hello, (c) workflow with no subjects.
      - `retro list`: all 3 rows present, deterministic across 2 runs (lexical by id), columns RETRO ID/SCOPE/TRIGGER/SUBJECTS/CREATED sensible, no-subject row renders cleanly (empty SUBJECTS).
      - `retro show` (a): scope/created + trigger/decision flat keys + 4 decoded subjects + body inline, no duplicate scope/subject lines.
      - `retro show` (b): `metadata:` block shows bench.1/eval.1/gate.1/note sorted lexically; body inline.
      - `retro show` (c): zero subjects, no crash, no empty subject lines.
      - Body fidelity: verified byte-for-byte via hexdump — a body with NO trailing newline is emitted verbatim (Fprint adds nothing).
      - Error paths: unknown id -> `retro "<id>" not found` exit 1; `bad/id` -> `invalid retro id "bad/id"` exit 1 AND same error when run outside any git repo (proves id validation precedes git/repo access); empty repo -> `no retros found` exit 0.
      - Throwaway repos cleaned up.
    
    QA findings:
    - correctness: Matches the approved plan exactly. Read-only over refs/etude/retros, mirrors run list/show, inline body dump (the documented divergence from run show), subjects decoded from indexed subject_run.N/bead.N, flat keys + sorted metadata block. No mutation, no new schema/namespace.
    - coverage: Adequate. Tests assert table columns, lexical list order, corrupt-manifest error, body round-trip, unknown/invalid id, workflow-no-subjects. No Verify-only gaps.
    - regressions: None. Full `go test ./...` green; scope limited to internal/cli/retro.go + retro_test.go.
    - code quality: Clean, self-contained, well-commented. gofmt/vet clean.
    - risks:
      - MINOR (cosmetic, non-blocking): subject ordering within a row puts bead.* BEFORE subject_run.* (lexical prefix sort: "bead." < "subject_run."), but the code comment on retro.go:206 says "subject_run before bead". Comment/behavior mismatch only; output is deterministic and self-consistent between list and show; no test asserts inter-prefix order. Recommend a Docs/follow-up comment fix, not an Implement return.
      - EXPECTED (handled in Docs, NOT a Verify failure): `make docs-check` is RED — only the new etude_retro_list.md / etude_retro_show.md pages + parent index lines are missing. `cmd/gen-docs TestDriftGuard` passes due to a known incomplete expectedCommands list. Docs phase runs `make docs` and extends gen-docs expectedCommands per the plan.
    
    Capture:
    - inputs: approved plan (bead --design) and Implement artifact (internal/cli/retro.go + retro_test.go diff)
    - output: append-only Verify note (this comment)

